# Normalize line endings to LF in the repo. The producer emits LF-terminated JSON; pinning EOL keeps
# committed derived/ artifacts byte-stable across machines (no CRLF/LF churn on Windows).
* text=auto eol=lf
*.png binary
*.jpg binary
*.svg text eol=lf
