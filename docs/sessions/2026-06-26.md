# Session 2026-06-26 — foundation + Phase 0 start

First build session. Terse log of what shipped, what's deferred, and next-up. (Doc-sweep SOP from `JD.md`.)

## Decisions (full rationale in DECISIONS.md)

- Name **Quadrature** (kept the working title).
- Verification model: **local SymPy producer + CI Node gates** incl. a parity oracle (ADR-0010).
- Reference build order: **breadth-first** after the Phase 0 slice (ADR-0007).

## Shipped

- Repo scaffold: configs (package.json, astro.config.mjs, tsconfig.json, deploy.yml), `.gitignore` fixed for
  the `producer/` layout.
- Neutrality fix: `QUADRATURE_BRIEF.md → PROJECT_BRIEF.md` (gitignored); brief named the board and would have
  published under the wrong filename.
- Documentation/governance system: AGENTS.md, DECISIONS.md (ADR-0001…0011), ROADMAP.md, CHANGELOG.md,
  README.md, LICENSE + LICENSE-content.md, and `docs/` (architecture, authoring-problems, authoring-formulas,
  regime-map, house-conventions, SOURCES) + this session log.

## In progress

- The SymPy producer (`producer/`), the schemas, the Node gates, and the Phase 0 throw-up slice (producer →
  player → build).

## Deferred / open

- Regime-2 ODE interactivity strategy (precomputed-sample-points export) — decide at the top of Phase 1.
- Reference breadth-fill — after Phase 0 review.

## Next up

Finish the producer + schema + gates, then the player + reference entry, then build + verify the DoD + commit
to a private GitHub repo (Pages left disabled until review).
