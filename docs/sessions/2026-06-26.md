# Session 2026-06-26 — foundation + Phase 0 start

First build session. Terse log of what shipped, what's deferred, and next-up. (Doc-sweep SOP from `JD.md`.)

## Decisions (full rationale in DECISIONS.md)

- Name **Quadrature** (kept the working title).
- Verification model: **local SymPy producer + CI Node gates** incl. a parity oracle (ADR-0010).
- Reference build order: **breadth-first** after the Phase 0 slice (ADR-0007).

## Shipped — Phase 0 vertical slice complete (functionally; awaiting review)

- Repo scaffold + configs; `.gitignore` fixed for `producer/`; `.gitattributes` (LF) so committed JSON is
  byte-stable.
- Neutrality fix: `QUADRATURE_BRIEF.md → PROJECT_BRIEF.md` (gitignored) + the `scan-text.mjs` gate.
- Documentation/governance system: AGENTS.md, DECISIONS.md (ADR-0001…0011), ROADMAP.md, CHANGELOG.md,
  README.md, LICENSE + LICENSE-content.md, and `docs/` + this session log.
- **SymPy producer** (`producer/`, uv): integrate a→v→x, solve the algebra register, prove the registers
  agree (tiered checker), unit-check, export closed form + parity samples, render the Matplotlib stacked SVG.
  Refuses to emit on a failed proof/unit check. 12 pytest physics cross-checks pass. (Caught + fixed a
  discriminant sign error by adding proof identities that verify the *answers*, not just internal
  consistency.)
- **Schemas + 5 Node gates** (CI, no Python): Ajv solution/reference, parity oracle (1e-9), KaTeX, scan.
  Each gate verified to bite.
- **Player** (Astro 7 + Svelte 5): four registers, honesty badges, the SHOWN proof, misconception register,
  sign rigor, interactive stacked graph (sliders, closed-form-in-JS), reference sheet, concept graph.
  Visually verified in a dev preview; no console errors.
- Initial commit pushed to **github.com/JD-Jones-ASES/Quadrature** (private).

## Deferred / open

- Regime-2 ODE interactivity strategy (precomputed-sample-points export) — decide at the top of Phase 1.
- Reference breadth-fill — after Phase 0 review.

## Phase 1 kickoff (same session, after Phase-0 review)

Owner reviewed the slice ("looks good"), approved the regime-2 interactivity recommendation, and cleared
autonomous work. Shipped:

- Post-review polish: fixed four Astro whitespace-gotcha dropped spaces; Matplotlib QC pass (per-panel
  limits, annotation bboxes, a–t panel scaled to include y=0).
- ADR-0012 (regime-2 interactivity: closed-form-in-JS default; `sampled` reserved) + ADR-0013 (regime-2
  verification = back-substitute into the equation of motion + ICs + invariant + memorized-result-falls-out).
- **Generalized the producer to ODE models**: a `models/` registry + a model-agnostic `Scenario`;
  model-agnostic `emit.py` / `graph.py`; the proof block generalized (`equivalence` | `governing`); a
  per-model `window_mode` (landing vs fixed) for the interactive graph.
- **Two regime-2 lessons, fully verified**: SHM (governing proof: solves x''=−ω²x, ICs, energy conserved,
  period falls out) and terminal velocity / linear drag (solves v'=g−v/τ, IC, x=∫v, terminal limit). Both
  interactive (cos/sin, exp) and parity-checked; visually confirmed (sinusoids 90° out of phase; v→asymptote,
  a→0).
- Reference breadth: 3 → 8 formulas (added shm-omega/x/period, drag-v/terminal); concept graph 8 nodes.
- Scalable lessons: dynamic `[slug].astro` route + a `/lessons/` index. 18 producer tests pass.

## Next up

Reference breadth-fill across mechanics (dynamics, energy, momentum, rotation, gravitation) per ADR-0007;
more lessons (damped oscillation is the next high-value regime-2 interactive). Pages still disabled until
publish.
